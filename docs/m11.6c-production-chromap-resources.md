# M11.6c — Full-reference Chromap resources and production runtime handoff

**M11.6c reviewed and accepted.** This resource/runtime qualification slice deploys
the exact accepted Chromap executable, builds and verifies both full production
indexes, publishes an immutable revision, and validates production lookup and
all five runtime dependencies. M11.6a/b parents, QC resources and scientific
contracts are preserved. No runtime/source/test code changed.

## Base and boundaries

`BASE_HEAD = 984d0a1065921336787fa0f52ec66177513b8e35`
(`Complete Milestone 11.6b production QC resources`). Initial branch `main`,
working tree clean, HEAD equal to local `origin/main`. Base commands were
`git status`, `git branch --show-current`, `git rev-parse HEAD origin/main`, and
`git log -3 --oneline`. No fetch, commit or push was needed/performed.

The frozen [M11.6a specification](m11.6a-production-resource-specification.md),
[M11.6b resources](m11.6b-production-qc-resources.md), README/AGENTS, Chromap
qualification record, and existing source/tests governed execution. Full-reference
construction is explicitly authorized by M11.6c; the builder's historical M11.2b
synthetic-only qualification statement is not treated as prior full-genome proof.
No alternate genome/backend, new index implementation, environment alias, tuning
parameter, automatic provisioning or biological workflow was introduced.

## Durable executable

| Field | Accepted value |
| --- | --- |
| Source executable | `/tmp/agent-m112b-qualification/candidate/chromap` |
| Durable executable | `/home/likeyi/program/agent-resources/runtimes/chromap/7ba402d0d69d08fedd525a4de83a4dd749670e88d4de507c236b07f03a285649/chromap` |
| Executable SHA-256 | `7ba402d0d69d08fedd525a4de83a4dd749670e88d4de507c236b07f03a285649` |
| Version | `0.3.2-r518` |
| Upstream commit | `5bd17e1f1c50805e76904efd021603cb6a1b6e23` |
| Backend policy | `chromap-atac-agent-support-v1` |
| Mapping policy SHA-256 | `25105d42733e5a57657df9577cade36c7709a8b6b63f743ab314b100769423a4` |
| Source archive SHA-256 | `0234cc870ef10367cde5b1510555ba4c06498461b69a3960b9697507ab4aa0b7` |
| Patch SHA-256 | `8d862e77d59213aa19b3fbc5f6014585a4b5f414f3e29143b68513ffd961df33` |
| Build record SHA-256 | `6bb93fb766aee7e752a71cbe36c305f7c8278b24699c6945fb6491d37ffa974a` |
| Platform | `Linux / x86_64 / little endian / 64-bit pointers` |

The trusted repository build record's own hash was verified. The existing
`identify_backend` checks passed at the temporary candidate, copied private stage,
and final durable executable: exact bytes, version, platform and linked libraries.
Only the accepted bytes were copied; no download, compilation or installation of
a replacement occurred. Publication used a fresh hash-named directory and fsync.
The retained build record identifies GCC 11.4.0, GNU Make 4.3, flags
`-std=c++11 -Wall -O3 -fopenmp -msse4.1`, and `-lm -lz` linkage. These are the
accepted historical build facts, not a claim to have rebuilt in this milestone.

| Linked library | Qualified SHA-256 |
| --- | --- |
| `libc.so.6` | `b2cf6c33b74d2f22543b7a469a75b538911e690f769d0b238843a49465b83793` |
| `libgcc_s.so.1` | `fc9d43b2f6c20e53b009238f767c5b949d202389e20de9e202ea684b4ba3729a` |
| `libgomp.so.1` | `d46f9225c1883039e8a6853e6d96ca1af11d034ce186a090952e4a7c8a7c2fdc` |
| `libm.so.6` | `3dd5511ae94785c9f921429b0f2b2f7aabb461b6f0e6de6dfdbef15f24bdfee6` |
| `libstdc++.so.6` | `ff0825e113603c3866680d5d52216bc6d8eedf3a59f52a0aef67ff01994db128` |
| `libz.so.1` | `64c206f0146cc58bbddc4f22054436f4ff278f5a554aa3ce6921ddf7e9133370` |

## Exact full-reference bindings

Both parents passed fresh `load_scatac_reference_bundle`, source reinspection,
and full `verify_fasta_fai` before construction. Every FAI name/length/order,
FASTA hash and line/offset geometry matched. Both builders repeated their
existing verification gates. Species/assembly bind through the validated parent
and its portable identity; the native index manifest does not invent extra fields.

| Field | Human / hg38 | Mouse / mm10 |
| --- | --- | --- |
| Parent portable identity | `763015ed6557b6f7a82202b047190f750fe224a66b1ebc27626e3916d6ac1085` | `bf698c7842fd86043840e80e0744345bcb11b6a979d917d33c73305e9d23f179` |
| Parent manifest SHA-256 | `20681078b31446df31c689992191d842d4110435004a5e547efea9696b59d7c4` | `95c2962bbfa1bfbadc5b3e24ad23d5dd6f9203371c3d6071484c69f1f1979408` |
| FASTA SHA-256 | `5be01555d98347fdb3714dc84c6f77c9d8bc774adcf32c6f7a8fa06f5baf5e51` | `db16cb4633191754f1d9cc70e73d2a1f60d03fdf62bcf4902a31a4717a3d2de7` |
| FAI SHA-256 | `3b425de206296a5c8053023fa5ca61da43cfe78c1737c12e58c83367c7e83c21` | `c08b7b0365da58adcd63373d67de08ac34707eaabad322951bf78ab8b8895927` |
| Ordered contig SHA-256 | `d06fcd768c9451398eb8e37d486c94e464efc5aca4991af4b05a423dcce3cd6f` | `dc112a6f0b63e7fddb4b864cbf456aa98276c98df3341a29982b9cd8b200798d` |
| Contig count | `455` | `66` |
| Native index SHA-256 | `9c246ea6b09894842e87567990a1633d608a7a97043fcda50335dc839b26397c` | `26d06016425a4624f360f03c58021c820684b667edc176eed8fd525d603ac880` |
| Native index bytes | `12552803536` | `11780233560` |
| Portable index identity | `ef7111acbe03f9c5e82d970ef26242482f6b53e660d3b719139076bef4ed6dae` | `569a09d0e777a5fb165a3c56dea727a40219e361fe44b97e1d511fa13a31a0f3` |
| Index manifest SHA-256 | `11671422e0abb28740d8c6e76c3d661d08a74d0c0b9323bdf8c2874b7cac7be6` | `cdb1bd14c7532f79c86e46f2cff352302c2182350959d0154b7c6fb856aaee03` |
| Build log SHA-256 | `220f8ed4b2744b7d93fb3a478f0b182fbd7877b870ef5a13524f2cffbca9249f` | `b9da561dad8074987c26f3c82be181e4ce1304f894f16cf4b0b8eee8c576caae` |

Full cCRE vocabularies remain 1,355,445 human and 1,341,077 mouse. They are
bound through the parent identity; Chromap indexes only the exact parent FASTA.
Both use `chromap-index-k17-w7.v1`, kmer 17, window 7, and the identical backend
identity above. No full-reference bound or scientific profile needed correction.

## Construction, exact commands and observations

The initial deployment audit found approximately 117 GiB available RAM and
1.7 TB free disk. Both build workspaces and the published root use the same
filesystem under the frozen resource root. Builds ran sequentially. These are
operator capacity observations, not new scientific thresholds or throughput claims.

### Human / hg38

- Build workspace: `/home/likeyi/program/agent-resources/build/chromap/m116-r1-human-attempt1`
- Completed build directory before publication: `/home/likeyi/program/agent-resources/build/chromap/m116-r1-human-attempt1/human-hg38`
- Final published directory: `/home/likeyi/program/agent-resources/indexes/chromap/m116-r1/human-hg38`
- Workflow start UTC: `2026-09-11T06:53:40.943781+00:00`
- Workflow end UTC: `2026-09-11T07:00:59.239280+00:00`

Exact native build argv, observed through a read-only Python subprocess audit hook:

```text
/home/likeyi/program/agent-resources/runtimes/chromap/7ba402d0d69d08fedd525a4de83a4dd749670e88d4de507c236b07f03a285649/chromap --build-index --ref /home/likeyi/program/agent-resources/sources/human-hg38/ucsc-hg38/hg38.fa --output /home/likeyi/program/agent-resources/build/chromap/m116-r1-human-attempt1/.human-hg38-zkbmth0r/reference.chromap --kmer 17 --window 7
```

Native log: indexing **110.30 s**, serialization **87.11 s**. Whole operator
construction/verification workflow: **438.30 s**. Peak child
RSS observation: **27,355,184 KiB** (Linux `RUSAGE_CHILDREN`).
This is a reaped-child high-water observation, not summed memory. The mouse
invocation may inherit the shell's earlier human child high-water mark, so its
value is not an independently isolated mouse peak. Observed peak
build file sizes: **12,552,805,922 bytes**; final published
directory: **12,552,805,922 bytes**. Disk observation polls
private build files, so it is an observed value rather than an exhaustive temporary
allocation peak. The final native sizes in the table come from completed manifests.

### Mouse / mm10

- Build workspace: `/home/likeyi/program/agent-resources/build/chromap/m116-r1-mouse-attempt1`
- Completed build directory before publication: `/home/likeyi/program/agent-resources/build/chromap/m116-r1-mouse-attempt1/mouse-mm10`
- Final published directory: `/home/likeyi/program/agent-resources/indexes/chromap/m116-r1/mouse-mm10`
- Workflow start UTC: `2026-09-11T07:01:00.633113+00:00`
- Workflow end UTC: `2026-09-11T07:07:30.699675+00:00`

Exact native build argv, observed through a read-only Python subprocess audit hook:

```text
/home/likeyi/program/agent-resources/runtimes/chromap/7ba402d0d69d08fedd525a4de83a4dd749670e88d4de507c236b07f03a285649/chromap --build-index --ref /home/likeyi/program/agent-resources/sources/mouse-mm10/ucsc-mm10/mm10.fa --output /home/likeyi/program/agent-resources/build/chromap/m116-r1-mouse-attempt1/.mouse-mm10-aivfcdlp/reference.chromap --kmer 17 --window 7
```

Native log: indexing **96.38 s**, serialization **76.74 s**. Whole operator
construction/verification workflow: **390.07 s**. Peak child
RSS observation: **27,355,184 KiB** (Linux `RUSAGE_CHILDREN`).
This is a reaped-child high-water observation, not summed memory. The mouse
invocation may inherit the shell's earlier human child high-water mark, so its
value is not an independently isolated mouse peak. Observed peak
build file sizes: **11,780,235,942 bytes**; final published
directory: **11,780,235,942 bytes**. Disk observation polls
private build files, so it is an observed value rather than an exhaustive temporary
allocation peak. The final native sizes in the table come from completed manifests.

The existing builder constructs in private sibling staging, verifies exact source
snapshots/backend before and after native execution, publishes within the build
workspace, and verifies the result. An explicit separate
`verify_chromap_reference_index` call then passed for each completed build.
No general native-index byte determinism is assumed. Verification authenticates
exact resource content and reference/backend/profile binding; it does **not**
independently reconstruct Chromap's historical internal build. Trusted construction
provenance is retained separately in exact argv, timestamps, logs and operator
records. The native manifest does not itself bind `build.log`.

## Publication and production lookup

Published root: `/home/likeyi/program/agent-resources/indexes/chromap/m116-r1`.

The operator held a publication lock outside this root, required a nonexistent
revision, rechecked complete manifest/native/log bytes, fsynced, and moved both
complete resource directories into a private sibling revision. One same-filesystem
rename published the complete two-species revision. Each directory contains only
`manifest.json`, `reference.chromap` and `build.log`. Builder locks/staging remain
outside the published root. No accepted artifact was overwritten.

Existing `verify_chromap_reference_index` passed again from both final locations
with exact expected parent and index manifest hashes and the qualified backend.
The root has exactly two regular child directories: `human-hg38` and `mouse-mm10`.
`scatac_fragments.resolve_execution`, the actual FASTQ public route's resolver,
uniquely selected each corresponding final manifest. No global activation occurred;
activation was confined to explicit operator validation subprocesses.

Lookup is not itself a native content verifier. The existing subsequent binding
verifier performs exact index/reference content checks. The operator checks
exercise those same APIs without manufacturing a biological intake workflow.
The following cases passed for **both species**, using disposable metadata and
same-size sparse corrupt-native fixtures, never mutations of published resources:

| Case | Expected rejection |
| --- | --- |
| Missing index / wrong-species index / wrong parent identity | `CHROMAP_INDEX_UNAVAILABLE` |
| Duplicate matching index | `CHROMAP_INDEX_AMBIGUOUS` |
| Wrong assembly / wrong parent manifest SHA | `FRAGMENTS_REFERENCE_MISMATCH` |
| Wrong index manifest SHA | `CHROMAP_INDEX_IDENTITY_MISMATCH` |
| Same-size corrupt native index | `CHROMAP_INDEX_IDENTITY_MISMATCH` at exact content verification |
| Changed backend identity / malformed child / symlink child | `CHROMAP_INDEX_INVALID` |
| Unset or nonexistent executable | `CHROMAP_UNAVAILABLE` |
| Changed executable bytes | `CHROMAP_EXECUTABLE_MISMATCH` |

All negative checks enforce the exact expected error code. Both published native
hashes were checked again after the probes. There is no PATH fallback, first-match
selection, recursive discovery or automatic scientific-execution index build.

## Bounded full-index usability

The existing accepted `mapping_argv` qualification interface consumed each full
published index using **one entirely generated 50-base paired read plus a 16-base
barcode**. Generation follows the existing tiny qualification harness:
`random.Random(112)` produces 12,000 ACGT bases; reads are positions 100:150 and
the reverse complement of 150:200. No read was taken from a biological dataset
or the production genome. Layout A and an explicit one-token whitelist were used.
All fixed flags, including one thread and the accepted support policy, were retained.

- Human: exit **0**, **6.30 s**, output **0 bytes**; full-reference and full-index load messages both confirmed.
- Mouse: exit **0**, **5.80 s**, output **0 bytes**; full-reference and full-index load messages both confirmed.

This proves the qualified executable can open/consume the actual full resources
and complete a bounded controlled invocation. It does not establish biological
mapping accuracy, fragment production acceptance or throughput. No Agent biological
FASTQ workflow, M11.7 execution, or M11.8 validation was run.

## Complete production runtime handoff

All five values below passed fresh runtime/resource checks:

| Variable | Exact lab-server activation value |
| --- | --- |
| `AGENT_QC_RESOURCE_CATALOG` | `/home/likeyi/program/agent-resources/catalogs/qc/9d9ea0340640fbab74f5f591831592ad3badf5973a09fcb988a7e2cba10e5eae.json` |
| `AGENT_QC_BEDTOOLS` | `/usr/bin/bedtools` |
| `AGENT_MATRIX_BEDTOOLS` | `/usr/bin/bedtools` |
| `AGENT_CHROMAP_BIN` | `/home/likeyi/program/agent-resources/runtimes/chromap/7ba402d0d69d08fedd525a4de83a4dd749670e88d4de507c236b07f03a285649/chromap` |
| `AGENT_CHROMAP_INDEX_ROOT` | `/home/likeyi/program/agent-resources/indexes/chromap/m116-r1` |

Both unchanged QC manifests and resource contents were freshly checked against
their accepted identities, and both returned `operator_qualified` under the exact
immutable catalog with synthetic opt-in unset. Existing QC and matrix BEDTools
qualification APIs separately passed their pinned executable/library/profile
checks; sharing `/usr/bin/bedtools` does not merge the scientific profiles.
The Chromap executable qualified again and both final indexes resolved uniquely.
The pinned GTF parser and existing sort/bgzip/tabix packaging runtime checks also
passed. No source/library upgrade or new pin was introduced.

Operator activation is explicit and process-scoped. On this reviewed lab server,
start a subshell, unset synthetic opt-in, export the five exact values above,
and then run only a separately approved Agent operation inside that subshell.
Do not edit shell startup files or replace immutable snapshot paths with aliases.
M11.7 execution is not authorized by this handoff record itself. These absolute
paths are deployment documentation, not portable scientific contract fields.

## Validation, evidence and repository integrity

Commands used the existing agent Python with `PYTHONDONTWRITEBYTECODE=1`,
`PYTHONPATH=src`, and the existing `/tmp/agent-numba-cache` and
`/tmp/agent-matplotlib-cache` for pytest. No runtime/source code change required
another full lightweight run; the accepted M11.6b full regression remains unchanged.

```text
python -B -m pytest -q tests/chromap/test_contracts.py tests/chromap/test_fragments_contracts.py tests/chromap/test_fragments_orchestration.py -p no:cacheprovider
198 passed in 20.81s

RUN_CHROMAP_QUALIFICATION=1 AGENT_CHROMAP_QUALIFICATION_RECORD=/tmp/m116c-test-build-identity.json python -B -m pytest -q tests/chromap/test_backend_qualification.py::test_tiny_index_build_reuse_and_repeatability tests/chromap/test_fragments_runtime_integration.py -p no:cacheprovider
5 passed in 32.32s
```

The guarded test record preserves accepted build identities and points its
candidate executable to the durable deployment. Those five existing tests use
only generated tiny references/reads and cover index reuse plus direct/DAG/
completed-resume/publication-recovery behavior. Full-reference usability above
is a separate operator probe. No tests were weakened or added to the repository.

External evidence root: `/home/likeyi/program/agent-resources/evidence/chromap/m116-r1`.
It retains `runtime.json`, per-species `*-build.json` and observed logs,
`publication.json`, `lookup.json`, per-species controlled probe inputs/logs/results,
`usability.json`, `runtime-handoff.json`, and exact operator procedures plus their
hash inventory in `procedures/inventory.json`. Published directories separately
retain the complete builder logs, whose hashes are in the build records.
These operator scripts call existing APIs; they are retained invocation evidence,
not a second index builder or a new repository provisioning interface.

At M11.6c submission, this bounded acceptance document was the sole repository
addition. Following acceptance, the final documentation-only M11.6 closeout
updates README/AGENTS to record completion and the M11.7/M11.8 boundaries.
No accepted resource, identity or scientific/runtime source code is changed. No biological,
reference, native-index or large-log payload enters Git. Final checks passed: `git diff --check`, Markdown links/fences, exact expected
untracked-file inventory, no tracked payload-like files, and registry introspection
(18 tools). Two positive lookups and 28 exact-code negative gates passed. Final
published native/manifest/log hashes and the retained evidence/procedure inventory
hashes all match. HEAD remains the recorded base; no files are staged.

## Scope, risks and deferred work

ToolRegistry = 18. Planner, compiler, v3/v4 wire schemas, Application, fragment
v2/FASTQ science, M11.4 QC/selection science and M11.5 matrix science are unchanged.
No accepted bound or backend contract needed correction. The deployment depends
on the exact qualified Linux libraries, retained parent/index/QC bytes and explicit
operator configuration; later drift must fail verification rather than trigger
fallback. Native content verification does not establish independent historical
build reconstruction or future arbitrary rebuild determinism.

M11.6 resource/runtime readiness is established. M11.7 remains real-data
operational end-to-end acceptance, including biological FASTQ execution and
throughput observations. M11.8 remains biological validation. Neither was begun.
No commit or push was made; stop for review.

## Milestone 11.6 documentation-only closeout

M11.6a, M11.6b and M11.6c have been reviewed and accepted. README/AGENTS now
record M11.6 completion as production resource readiness and runtime qualification,
with the accepted annotation-only engineering bound and unchanged scientific
semantics. M11.7 is Real-Data End-to-End Production Acceptance; M11.8 is Biological
Validation & Raw Preprocessing Closeout. Neither is begun here.

Final documentation/static checks passed: links, fences, whitespace,
`git diff --check`, 18-tool registry introspection, no tracked production/reference/
index payload, and only README, AGENTS and this acceptance record in the candidate
diff. No resource was rebuilt or modified, no scientific/runtime source changed,
and no full lightweight suite was rerun for this documentation-only closeout.
Nothing is staged; HEAD remains `984d0a1065921336787fa0f52ec66177513b8e35`.

MILESTONE 11.6 FINAL ACCEPTANCE READY

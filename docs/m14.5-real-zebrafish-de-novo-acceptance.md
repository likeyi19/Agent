# M14.5 — Real zebrafish de-novo technical acceptance

## Baselines and scientific scope

Agent baseline: `a62a45100385baf05914e072cb8d4a78cf3dfcce`. EpiZoo remains unchanged at `029cd631d0f9806a646c4a3b42ce10b958f2b67f`. Registry remains 22. No human/mouse embedding, mapping, Planner algorithm, catalog ceiling, compression, wire schema, model architecture, trainable-module set or objective changed. No commit or push has been performed; closeout awaits user review.

This milestone qualifies the operator-approved real public zebrafish binary demo for bounded de-novo technical execution. It does not establish biological convergence, embedding quality, downstream cell-type accuracy, superiority, production-scale sufficiency or interchangeability of binary accessibility and fragment counts. No upstream fragment/QC/selection authority, public target-model inference or exact interrupted-training resume is added.

## Explicit binary adaptation contract

The original `epizoo-species-posttraining.v1` fragment-count scientific profile and SHA-256 `a99300277b315218afeb4812d6a51ba31eb36ea43dca28711344c4ccbaa483e0` are unchanged. Original `qualification.v1` and `production-candidate.v1` public names continue to require `fragment_counts`.

M14.5 adds `epizoo-species-posttraining-binary-de-novo.v1`, selected explicitly in the existing `epizoo-adaptation-inputs.v1` specification by `execution_profile="binary-de-novo-qualification.v1"`. It requires exact `binary_accessibility`, `strategy="de_novo"`, null mapping and qualification purpose. Binary mapped-reference and binary production execution are rejected. `insertion_counts` remains unsupported by adaptation. Profile selection is never inferred from the species, strategy or numeric values. Mixed-semantic joint corpora remain rejected.

The new scientific profile differs only in identity and matrix semantics; its bounded execution uses the same EpiZoo preprocessing, architecture, sequence handling, SR+CCA objectives, precision and trainer as fragment qualification. The two semantics remain distinct in the source matrix identity and selected profile; numeric or biological equivalence is not asserted. The existing manifest, receipt, result validation and scientific-authority paths now bind the selected exact profile hash. There is no new authority engine or recovery protocol.

The scientific authorization is the data-owner declaration in `operator-declaration.txt`: the real binary demo is approved for this de-novo technical workflow. Historical binarization/count-production reconstruction was explicitly waived; values are not relabeled as fragment counts.

Operator declaration SHA-256: `bad0c7183ecc7b722e17db12aff7d0ecb28a6989b9d507f34ae88f9097bba715`. Exact source/test patch against the Agent baseline: `4349d07e0b2c7d9f592eae03031e475dabc0a1f8a160c7bb0de4473a4766440c` (`source-and-tests.patch`).

## Exact real resources and representation preparation

Target: **Danio rerio**, NCBI taxonomy **7955**, assembly **danRer11**. The public EpiZoo demo names danRer11. The operator supplied the exact FASTA/FAI under `/home/likeyi/program/agent-resources/epizoo_species_adaptation/danRer11/reference`. Existing M14.1 full streaming validation checked all 1,923 contig names, lengths, order and FAI byte/line geometry. The supplied FAI was valid and was not regenerated. Every matrix-derived interval passed exact contig and bounds checks.

| Resource | SHA-256 |
| --- | --- |
| Original danRer11.fa | `9d45886b4c686f5fef63e53f14a02277c0c4467d46982dd8dee742420f84661a` |
| Original danRer11.fa.fai | `8be439d7b9c529203a0adf64ca9183b6f72b66d3f2c0bd8adc0924e8e56818aa` |
| Original zebrafish_downsampled_2000_cells.h5ad | `1e1d61970cca6225fb5c39e122a6fcea8e2e86c8c66dafbca8594ef6d1f96205` |
| Prepared H5AD | `9a23e66ae06dcd335070481b649db447992daae18841b448bb513d9f3d91fcac` |
| Derived ordered BED | `390243c5dd7e692e7af6a01be8f9f2673fd95a9fc47c1b8a4fdcb076400b967e` |
| Reference manifest | `9dc34c071b009da4f2ed5512daa391b452906cc6d25610610675d7c69f41ceb4` |

Reference identity: `6d60b56a0c11a65548a2fa1fa070cb2944021b4139a19b143245cf998a6c43b4`. Ordered feature identity: `da73e2d187bf2c00063c115824ed29500f677ff821e181a8f3832a054b131069`. Ordered cell identity: `76349ca239fa1eb212bec71de96729b048a4b2c96663ff1f26b9a00fbd2db344`. Logical matrix identity under binary semantics: `5d8bef6baf3a9be0015583b75b799742d32e4d682d66bcbb17965a54a2267e3c`.

The source is `/home/likeyi/program/EpiZoo/data/zebrafish_downsampled_2000_cells.h5ad`, **2,000 × 466,307**, canonical float32 CSR. All **2,970,369** nonzeros equal 1.0; total is 2,970,369 and there are no zero rows. The complete unique var_names axis defines 500 bp regulatory regions on chr1–chr25. It was parsed to `features.bed` without sorting, deduplication, renaming, clipping, merging or projection. This is a matrix-derived regulatory vocabulary, not a fabricated curated-cCRE authority.

The prepared H5AD keeps all 2,000 cells and all 466,307 features, with no subsampling. Every 1.0 converts exactly to signed-int64 1. Exhaustive re-reading proves identical CSR indptr, indices and values, shape, both axis orders, nnz and total; sorted unique CSR and absent explicit zeros are verified. Index-only obs/var and explicit species/assembly/binary metadata satisfy the strict contract. Original annotation metadata remains in the unchanged public demo file. Original-versus-prepared logical matrix digests agree.

## Real model resources and memory gate

Pretrained checkpoint SHA-256: `6b2d13fdbd54a9b0d56efa5afa81bc4832b813f4eac8662e4d93cc08c9d9b39a`. Real SEAM checkpoint SHA-256: `d40b5626622a302f693f2e36ad4f4942e83cf6daf602e1add5216f72b75e2530`. EpiZoo source code and all weight/configuration resources are unchanged from M14.2/M14.4.

The old bundles also bind Agent adapter bytes. They correctly rejected the new adapter before Application/scientific execution. Existing provisioning requalified the same weights and EpiZoo code, producing source bundle identity `b2077733fd0f9e2fb770f918980236bda81f7c4a7cfaa0f64fbc1cd69d5d0c35` and SEAM bundle identity `835ecfa0629a665801aa734d3ae9c37e2b542ce70acb19ab493c395a12339ad0`. This includes one bounded CPU SEAM compatibility forward, separately recorded from the real complete-target SEAM production. Existing bundles remain immutable.

Full-vocabulary meta inspection: 1,004,651,004 parameters; 765,899,772 trainable; 238,751,232 frozen sequence parameters. FP32 parameters, gradients and Adam moments require 13,209,401,280 bytes before activations, AMP caches, temporary allocations and CUDA overhead. No feature reduction or alternative architecture is used.

## Public execution, preprocessing and verification

Public DAG: `adopt_scATAC_cell_by_features` → `adapt_epizoo_species(strategy=de_novo)`. Scripted scope selection and semantic compilation feed the existing AgentApplication. Source/output conservation belongs to the matrix owner; target adaptation belongs to the EpiZoo adaptation owner. Downstream presentation and completed-run recovery reuse schema-2 authority.

EpiZoo-owned `compute_document_frequency`, `compute_tfidf` and `generate_cell_sentences` use exactly the accepted 2,000-cell binary corpus. TF-IDF remains scale 10,000/float32; sentences retain the existing offset-4, target-species-neutral tokenization, pinned ranking and 8,192-token/random-truncation behavior. Eight real cells exceed 8,190 accessible features and retain the existing random sentence truncation; all model columns remain intact. Ten bounded batches do not imply updating on every corpus cell. No human/mouse corpus DF, alternative column-total formula or Agent TF-IDF implementation is introduced.

Real SEAM consumes exact ordered danRer11 DNA for all 466,307 features, with strict extraction and no skips or repairs. De-novo transfer inherits the transformer/MoE, rank, CCA and shared/special parameters. Acceptance observation hashes the native target identity and decoder initialization at production time and checks the returned rows against those hashes; it does not initialize a second model or consume extra RNG draws. EpiZoo qualification checks shared inheritance and real target sequence binding; only sequence embeddings are frozen. No mapping function may execute.

Completed run: `m14.5-real:run`. Public result: successful. Scientific profile SHA-256: `245bb5b6fa49d4df3b2876036c35eab9b4c100886709724c953ee801444b1c80`. Target DF SHA-256: `c5298ebd5f95d916956eb30af8c5e9cee3dca57554e5706fdb9287114ae1db35`. Target sequence embedding SHA-256: `cffe3fc883438ad7b9ba7096538574ad84472af5721cb1c1d6ded10439f93c93`.

Real SEAM produced finite float32 **466,307 × 512** embeddings in **547.67 seconds**. Cell batch **1**, SEAM batch **4**, seed **0**. Trainer attempts **10**, applied optimizer updates **10**, AMP skips **0**; all recorded losses finite. Complete Application runtime **719.96 seconds**. Peak GPU allocated **21,109,670,912 bytes** (19.660 GiB), reserved **23,089,643,520 bytes** (21.504 GiB). The lightweight regression ran concurrently for part of the acceptance, so wall time is an observation, not an isolated throughput benchmark.

Checkpoint SHA-256: `1de0a9d7c391a8eba55b307f3318af0a96b65e7d2b01038af23d73f150f1d257`. Target manifest SHA-256: `fcb309b7d4e78bd8c0fc9ace8f7eb4e3db6b57653c0e200788ff43bb98517caa`. Strict reload and finite forward passed with a 512-dimensional cell embedding and full 466,307-column decoder.

Observed production/owner counts: `{"adaptation_owner": 1, "initialization": 1, "mapping_production": 0, "matrix_owner": 1, "matrix_production": 1, "seam_production": 1, "training": 1}`. These counts exclude the earlier CPU-only resource provisioning check. Matrix owner and adaptation owner each prove their own scope once; no mapping, second SEAM production or training replay occurs.

Target manifest: `/home/likeyi/program/agent-acceptance/m14.5/application-r2/application/runs/50278ffc0df486c9291890c913ceecab2c5f23520a7a56eebf3cbe4072277035/scientific/epizoo-adaptation-523bafa068deec189e36aed080124c3beafabfe647322e51eca009401a5cf882/artifact/manifest.json`.

Evidence: `/home/likeyi/program/agent-acceptance/m14.5/application-r2/application/runs/50278ffc0df486c9291890c913ceecab2c5f23520a7a56eebf3cbe4072277035/evidence/analysis_evidence.json`.

Report: `/home/likeyi/program/agent-acceptance/m14.5/application-r2/application/runs/50278ffc0df486c9291890c913ceecab2c5f23520a7a56eebf3cbe4072277035/report/analysis_report/analysis_report.md`.

Fresh-process completed-publication recovery passed under hard guards: zero adoption/matrix production, matrix-owner reconstruction, SEAM, mapping, initialization, training or adaptation-owner reconstruction. Appending bytes to the published DF artifact invalidated reuse. Restoring its exact bytes/hash restored successful recovery; run state was unchanged. Original matrix, genome, FAI and weights were not mutated.

## Validation and implementation scope

Focused Agent reference/adoption/adaptation/public-integration checks: **180 passed**, two existing warnings (10.53 seconds). Catalog/benchmark checks: **118 passed**, unchanged ceilings (83.33 seconds). EpiZoo qualification checks: **21 passed**, two existing warnings (2.91 seconds). Unit tests cover both semantics, unsupported declarations, binary mapping/production rejection, self-consistently rehashed scientific forgeries, profile/receipt/authority binding and publication recovery. Mocked unit artifacts do not claim real execution.

One complete lightweight Agent run: **4125 passed, 83 skipped, 7 warnings in 966.07s (0:16:06)**, exit **0**, all RUN_* gates unset, no test exclusions. Source/test/benchmark tree unchanged during this run: **True**.

Production changes are limited to six files: adaptation contract/profile validation; corpus semantic admission; publication/receipt profile binding; the public specification adapter; adaptation authority profile binding; and registry result validation/planning description. Five existing test files add or extend focused coverage. EpiZoo source, matrix contracts/algorithms, model backend computations, Planner algorithms, provider limits and catalog ceilings are unchanged.

Attempt history: the first Application launcher stopped during resource binding before matrix/SEAM/training because the old bundles pinned old Agent adapter hashes. A fresh `application-r2` uses newly qualified bundles; it is not a resumed-training claim. Two early new test assertions mishandled immutable mapping proxies and were corrected in tests only. The existing readiness-r1 is retained as historical evidence, superseded by the supplied genome and explicit binary declaration.

All biological data, genomes, checkpoints, preparation scripts, observations and execution artifacts remain under `/home/likeyi/program/agent-acceptance/m14.5` or the supplied resource paths, outside Git. Detailed identities/proofs are in `prepared-r1/`, `model-bundles-r1/`, `application-r2/`, and `full-suite-tree.json`. Pre-existing `evals/` remains unrelated and untouched. No next milestone, commit or push was started.

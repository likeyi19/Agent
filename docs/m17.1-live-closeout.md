# M17.1 — Live-provider acceptance and closeout regression

M17.1's bounded implementation remains architecture-aligned. This closeout made
**no production or test changes**. It does not implement M17.2, add an adapter,
change Planner semantics, or repair provider output.

**M17.1 software acceptance passed and the milestone is ready to close and
commit/push.** Separately, **live provider qualification for DashScope
`deepseek-v4-flash-0731` did not pass the tested real Agent schemas and remains
unresolved outside the M17.1 software milestone.**

These independent conclusions follow the established architecture principle:
**Agent correctness and provider/model quality/compatibility are evaluated
separately.** The live evidence below is preserved without weakening its failure
classifications or the strict Agent response contracts.

## Baseline and diff review

Agent HEAD remains `4087167fec9c139ab1896e1dae3cb60e736974c9`, with the reviewed
M17.1 implementation uncommitted and existing `evals/` preserved. Registry remains
23. EpiZoo remains clean at `029cd631d0f9806a646c4a3b42ce10b958f2b67f`.

The application still has one M15/M16 interaction architecture and one executable
Planner. Read-only guidance imports the existing Registry catalog projection and
M16 evidence/context/rendering machinery; it does not invoke the Planner,
preflight, scientific owners or reconstruction. Heterogeneous evidence bypasses
comparison construction without relaxing M16.3 compatible-comparison admission.

Candidate completion records contain exact references, not generated rationale,
readiness conclusions or scientific interpretation. There is no GuidancePlanner,
recommendation engine, biological workflow graph, guidance authority/store,
synthesis engine, deterministic biological recommendation rules, autonomous
execution or provider-specific workaround. The existing free-prose
entailment limitation remains explicit; it was not redesigned during closeout.

## Provider and preserved evidence

Live calls used the unchanged `OpenAIPlanningModel.complete(prompt,
response_schema)` implementation with an injected official OpenAI client and
DashScope configuration supplied by `conda run -n agent`:

- model: `deepseek-v4-flash-0731`;
- transport: `responses.create` with existing strict `text.format` JSON Schema;
- unchanged `store=False`, `background=False`, completion-status and `output_text`
  handling;
- adapter timeout 60 seconds, SDK retries zero;
- two-call maximum per case, no response substitution or prompt retry.

Credentials were not printed, logged or persisted. No provider production changes
or reasoning settings were introduced. The existing `PlanningModel` interface
remained the sole generation boundary.

The source was the preserved real `live-annotation` session from M16 acceptance,
backed by `evals/user_acceptance_2026-09-16/workspaces/f2-execute`. Its accepted
annotation summary was available. It contains exact groups `3` (Monocytes) and `7`
(ActNK), used for the objective question without claiming biological equivalence.
Only isolated session metadata was copied under `outputs/m17.1-closeout/`.
Scientific publications were read in place, not regenerated.

## Live cases

| Case | Prompt / intent | Observed result | Calls |
| --- | --- | --- | ---: |
| G1 | “What should I analyze next?” | `invalid_decision` clarification. Provider omitted the required turn envelope and returned `targets: ["r0"]` instead of target objects. No candidate admitted. | 1 |
| G2 | Investigate why accepted cluster 3 differs from cluster 7 | `PROVIDER_TIMEOUT` at 61.36 seconds; application returned clarification. No admitted guidance. | 1 |
| G3 | “Why would the first option help?” | Not exercised: G2 produced no live candidate. No scripted replacement or regenerated candidate was substituted. | 0 |
| G4 | “What evidence supports suggesting that?” | Not exercised: no valid G2/G3 guidance predecessor. | 0 |
| G5 | “Option 1 sounds right. Do that.” | Provider attempted an unsupported-answer decision but omitted the envelope and used invalid fields. Agent clarified without planning or execution. This confirms rejection safety, not a successful live stable-candidate handoff-boundary test. | 1 |
| P1 | PLAN_ONLY: “Inspect the supplied scATAC-seq dataset.” | Provider returned bare `processed_inspection`, not the required scope-selection JSON. Existing Planner rejected `INVALID_PLANNING_SCOPE`; no plan was constructed and preflight was not reached. | 1 |
| D1 | Interpret the accepted annotation result for cluster 3 | Provider omitted the turn envelope and returned fields such as `output_handle`/`subjects` instead of the required target. Agent clarified `invalid_decision`. | 1 |
| D2 | Existing M16 limitations follow-up | Not exercised because D1 established no successful live discussion predecessor. | 0 |

Total: **five live calls**, no retries/fallbacks, no successful guidance or M16
generation calls. Returned malformed completions passed the existing terminal
status/output-text adapter checks, then failed application/Planner parsing.

The turn-decision schema was sent in G1/G2/G5/D1. The scoped Planner selection
schema was sent in P1. Neither real schema was successfully satisfied. Guidance
candidate-generation, M16 explanation-generation and detailed Planner schemas
were not reached. The earlier minimal strict-object qualification remains valid
only for that earlier representative object; it did not establish these schemas.

## Failure classification

G1, G5, P1 and D1 are **observed provider structured-response contract failures**,
not merely awkward scientific prose. They do not show a Responses transport
incompatibility. This bounded run cannot determine whether the service failed to
enforce schema constraints or the model failed to honor them. No provider-specific
normalization, envelope insertion, field repair or scientific architecture change
was applied.

G2 is an external provider timeout. Scientific reasoning/prose quality cannot be
assessed from successful answers because generation was not reached. All invalid
responses failed closed. No invented capability/result/readiness fact or execution
was admitted; no Agent correctness defect was exposed by these cases.

**Representative existing Agent LLM-facing capability reuse through DeepSeek was
not established.** In particular, P1/D1/D2 did not pass. This is a separate provider
qualification result from deterministic M17.1 architecture correctness.

## Scientific invariance

Conversation cases guarded Registry execution, runtime run/resume, scientific
verification/presentation, Planner entry and executor preflight. No guard was
triggered. P1 separately permitted ordinary planning/preflight while guarding every
scientific Registry callable; it constructed no plan and ran no tool.

SHA-256 snapshots before the live phase and after the full regression preserved all
**404 files** covering
current production/tests, the original accepted workspace and original session
metadata. The original scientific workspace contains **25 files / 499,293,671
bytes**. Only isolated new interaction records and ignored acceptance diagnostics
were written. Scientific algorithms, accepted artifacts, authority semantics,
Planner/compiler execution semantics and EpiZoo were unchanged.

The prior 40-test dedicated gate includes real-owner guidance cases with zero
instrumented scientific work; the prior 502-test broader focused regression passed.
These gates were not separately repeated during closeout because no code changed;
the full lightweight run includes them.

## Full lightweight regression

The full suite passed: **4,631 passed, 83 skipped, 7 warnings in 2,998.74 seconds**
(49:58), exit 0, **zero failures/errors**. Wrapper wall time was 3,001.02 seconds.
All **378 production/test files** matched their frozen hashes after execution.
The seven warnings concern package deprecations, the unavailable TBB threading
layer version, and intentional duplicate-observation-name fixtures.
Final `git diff --check` passed; the index remained empty, Agent HEAD remained at
the baseline, and EpiZoo remained clean at its pinned HEAD.

The normal full test command ran without exclusions, with all inherited `RUN_*`
gates removed. The wrapper
sets `PYTHONPATH=src`, `PYTHONDONTWRITEBYTECODE=1`,
`NUMBA_CACHE_DIR=/tmp/agent-numba-cache` and
`MPLCONFIGDIR=/tmp/agent-matplotlib-cache` and verifies production/test hashes.

```bash
/home/likeyi/anaconda3/bin/conda run --no-capture-output -n agent \
  python -B outputs/m17.1-closeout/regression.py
```

Child command:

```bash
/home/likeyi/anaconda3/envs/agent/bin/python -m pytest -q tests \
  --junitxml=/home/likeyi/program/Agent/outputs/m17.1-closeout/full.xml
```

Ignored records include per-case schemas/responses and sanitized error codes,
results, preservation hashes, full-suite command/log/JUnit XML and result metadata.
No secret values are included. Nothing was staged, committed or pushed.

## Closeout verdict

**M17.1 software acceptance: PASS — ready to close and commit/push.** The dedicated
guidance gate (40 passed), broader focused regression (502 passed), and full
lightweight regression (4,631 passed, 83 skipped, zero failures/errors) support
this verdict. Whitespace checks passed, scientific artifacts and accepted state
remained invariant, and no unintended scientific work or M15/M16/Planner/authority
regression was found. M17.1 architecture boundaries remain intact.

**DeepSeek provider qualification: NOT PASSED / UNQUALIFIED.** DashScope
`deepseek-v4-flash-0731` successfully used the Responses transport but did not
reliably satisfy the tested real Agent structured-response contracts. This is a
structured-response contract reliability limitation, not merely weak prose and
not an Agent architecture defect. Qualification remains unresolved outside the
M17.1 software milestone.

The failures affect the pre-existing Planner scope-selection contract (P1) and
M16 interpretation decision contract (D1), as well as guidance decisions (G1/G5);
G2 separately timed out. Together with passing software regression, this supports
classification as a provider qualification failure across existing LLM-facing
interfaces rather than an M17.1-specific regression. Malformed responses failed
closed: no invalid candidate or plan was accepted, no unexpected science ran,
and no Planner/execution guard was bypassed. The unsuccessful live qualification
does not block M17.1 software acceptance.

Representative existing Agent LLM-facing capabilities have **not** been shown to
successfully reuse DeepSeek in this closeout. The earlier minimal Responses
qualification does not establish full-schema compatibility. No adapter change
is justified solely by the malformed completions observed here; no transport
failure requiring another API was established. M17.2 remains unimplemented.

This reclassification changed only this closeout document. All 378 source/test
files still match the completed full-regression snapshot; the full suite was not
rerun. `git diff --check` and tracked diff/status were checked again. No further
provider/model testing, schema simplification, retry, repair, prompt branching or
DeepSeek-specific logic was added. Provider qualification is separate follow-up
work after M17.1 closure. Nothing was staged, committed or pushed.
